# ansible/roles/app/defaults/main.yml
# Default values — can be overridden in group_vars or playbook vars.

app_name: taskmanager
app_dir: /opt/taskmanager
app_port: 3000
node_env: production
docker_image: "{{ dockerhub_username }}/taskmanager-api:latest"